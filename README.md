# 🎬 Custom HTML5 Video Player

A custom-built HTML5 video player using **HTML**, **CSS**, and **Vanilla JavaScript**.  
This project demonstrates how to create your own video controls instead of relying on the browser’s default UI.

## 🚀 Features

- ▶️ Play / Pause toggle
- ⏩ Skip forward and backward buttons
- 🔊 Volume control
- ⚡ Playback speed control
- 📊 Interactive progress bar with scrubbing
- 🖥️ Fullscreen responsive layout
- 🎨 Modern UI with custom styling
- ⛶ Supports fullscreen mode using the Fullscreen API

## 📂 Project Structure

├── index.html
├── style.css
├── scripts.js
├── 652333414.mp4
└── README.md

## 🛠️ Technologies Used

- HTML5 (`<video>` element)
- CSS3 (Flexbox, gradients, custom range inputs)
- JavaScript (DOM manipulation & media API)

⚙️ How It Works
The HTML defines the video player structure and controls.

CSS styles the player, sliders, and progress bar.

JavaScript:

Controls playback (play(), pause())

Updates the progress bar in real time

Handles skipping, volume, playback speed, and scrubbing

▶️ How to Run Locally
Clone the repository:

bash
Copy code
git clone https://github.com/mritunjay00111/html-video-player.git
Open index.html in your browser.

Make sure the video file is in the same directory.

📌 Future Improvements
Add fullscreen toggle button

Keyboard shortcuts (spacebar, arrows)

Mobile touch gestures

Playlist support


📄 License
This project is open-source and free to use.
